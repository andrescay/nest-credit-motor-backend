PORT=3000
NODE_ENV=production

# Today the app uses in-memory repositories.
# Keep this variable for future switch to Mongo-based repositories.
STORAGE_PROVIDER=in-memory

# Planned future use when Mongo repositories are implemented.
MONGODB_URI=mongodb://localhost:27017/credit_motor
